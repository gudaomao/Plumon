<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div id="topnav">
        <div class="navbar-header">
            <a class="navbar-brand" href="/">Plumon!</a>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li<% if(path && path=='/'){%> class="active"<%} %>><a href="/">首页</a></li>
                <li<% if(path && path=='/new'){%> class="active"<%} %>><a href="/new">新建代码段</a></li>
                <% if(u && u.name){%><li<% if(path && path=='/my'){%> class="active"<%} %>><a href="/my">我的</a></li><%}%>
                <% if(path && path=='/tag/'){%><li class="active"><a href="#">标签</a></li><%}%>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li<% if(path && path=='/reg'){%> class="active"<%} %>><a href="/reg">注册</a></li>
                <li>
                    <form class="navbar-form" role="search">
                        <div class="form-group">
                            <input type="text" class="form-control" placeholder="Search">
                        </div>
                        <button type="submit" class="btn btn-default">搜索</button>
                    </form>
                </li>
            </ul>
        </div>

    </div>
</nav>